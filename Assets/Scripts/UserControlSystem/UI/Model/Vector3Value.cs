using System;
using UnityEngine;

namespace UserControlSystem.UI.Model
{
    [CreateAssetMenu(fileName = nameof(Vector3Value), menuName = "RTS/ " + nameof(Vector3Value), order = 0)]
    public sealed class Vector3Value : ScriptableObjectBase<Vector3>
    {
        
    }
}